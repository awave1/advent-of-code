{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "(Windows) Launch",
      "type": "cppvsdbg",
      "request": "launch",
      "program": "${workspaceRoot}/target/debug/advent-of-code.exe",
      "args": ["--day", "3", "--test"],
      "stopAtEntry": false,
      "cwd": "${workspaceFolder}",
      "environment": [],
      "externalConsole": false,
      "preLaunchTask": "cargo build"
    },
    {
      "name": "(OSX) Launch",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceRoot}/target/debug/advent-of-code",
      "args": ["--day", "3", "--test"],
      "cwd": "${workspaceRoot}",
      "preLaunchTask": "cargo build"
    }
  ]
}
